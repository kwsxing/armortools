name: macOS (Metal)

on:
  push:
    branches:
    - main
    - kws
  pull_request:
    branches:
    - main
    - kws

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v3
    - name: Compile
      run: |
        cd paint
        ../base/make --compile
